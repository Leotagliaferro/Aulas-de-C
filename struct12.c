#include <stdio.h> 

struct Numero {
    int par[10];
    float soma;
};

int main () {
    struct Numero numero;
    int i; 

    for (i=2;i <= 20; i+=2 ) {
        numero.par;
    }

    printf("o array e: %d", numero.par[i]);
    
    
    return 0;
}